<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Lunchtime</title>
        <%= stylesheet_link_tag "application" %>
    </head>
    <body>
        <script type="text/x-handlebars">
            <div class="navbar">
                <div class="navbar-inner">
                    <a href="" class="brand">Lunchtime</a>
                    <ul class="nav">
                        {{#linkTo 'lunch.index' tagName='li'}}
                        <a {{bindAttr href="view.href"}}>Lunches</a>
                        {{/linkTo}}
                        {{#linkTo 'companies' tagName='li'}}
                        <a {{bindAttr href="view.href"}}>Companies</a>
                        {{/linkTo}}
                        {{#linkTo 'signin' tagName='li'}}
                        <a {{bindAttr href="view.href"}}>Sign In</a>
                        {{/linkTo}}
                    </ul>
                </div>
            </div>
            <div class="container">
                <div class="row">
                    <div class="span2">
                        {{outlet sidebar}}
                    </div>
                    <div class="span10">
                        {{outlet}}
                    </div>
                </div>
            </div>
            <div class="container">
                <div class="row">
                    <div class="span8 offset2 text-center" style="color: #aaa;">
                        <hr style="margin-bottom: 0;" />
                        <% adjectives = %W[ acceptable adequate bearable decent livable passable tolerable ] %>
                        <small>Another <%= adjectives.sample %> product brought to you by Caffeinated Industries</small>
                    </div>
                </div>
            </div>
        </script>
        <%= javascript_include_tag "application" %>
    </body>
</html>
