<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Lunchtime</title>
        <%= stylesheet_link_tag "application" %>
    </head>
    <body>
        <script type="text/x-handlebars" data-template-name="lunches">
            <div class="navbar">
                <div class="navbar-inner">
                    <a href="" class="brand">Lunchtime</a>
                    <ul class="nav">
                        <li class="active"><a href="">Lunches</a></li>
                        <li><a href="#/companies/">Companies</a></li>
                    </ul>
                </div>
            </div>
            <div class="container">
                <div class="row">
                    <div class="span2">
                        what
                    </div>
                    <div class="span10">
                        <table>
                            <tr>
                                <th>Active</th>
                                <th>Name</th>
                            </tr>
                            {{#each controller itemController="lunch"}}
                            <tr>
                                <td>
                                    {{view Ember.Checkbox checkedBinding="active" class="toggle"}}
                                </td>
                                <td>
                                    {{#if isEditing}}
                                    {{view Lunchtime.EditLunchView valueBinding="name"}}
                                    {{else}}
                                    <label {{action "editLunch" on="doubleClick"}} {{bindAttr class="active::inactive"}}>{{name}}</label>
                                    {{/if}}
                                </td>
                            </tr>
                            {{/each}}
                        </table>
                        <div class="input-append">
                            {{view Ember.TextField id="new-lunch" placeholder="New lunch"
                                    valueBinding="newName" action="createLunch"}}
                            <button class="btn" {{action "createLunch"}}>Add</button>
                        </div>
                    </div>
                </div>
            </div>
            <div class="container">
                <div class="row">
                    <div class="span8 offset2 text-center" style="color: #aaa;">
                        <hr style="margin-bottom: 0;" />
                        <% adjectives = %W[ acceptable adequate bearable decent livable passable tolerable ] %>
                        <small>Another <%= adjectives.sample %> product brought to you by Caffeinated Industries</small>
                    </div>
                </div>
            </div>
        </script>
        <%= javascript_include_tag "application" %>
    </body>
</html>
