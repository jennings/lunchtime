<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Lunchtime</title>
        <%= stylesheet_link_tag "application" %>
    </head>
    <body>
        <script type="text/x-handlebars" data-template-name="lunches">
        <div class="navbar">
            <div class="navbar-inner">
                <a href="" class="brand">Lunchtime</a>
                <ul class="nav">
                    <li class="active"><a href="">Lunches</a></li>
                    <li><a href="#/companies/">Companies</a></li>
                </ul>
            </div>
        </div>
        <div class="container">
            <div class="row">
                <div class="span2">
                    what
                </div>
                <div class="span10">
                    <table>
                        <tr>
                            <th>Active</th>
                            <th>Name</th>
                        </tr>
                        {{#each controller}}
                        <tr>
                            <td>
                                {{view Ember.Checkbox checkedBinding="active" class="toggle"}}
                            </td>
                            <td>
                                <label {{bindAttr class="active::inactive"}}>{{name}}</label>
                            </td>
                        </tr>
                        {{/each}}
                    </table>
                    {{view Ember.TextField id="new-lunch" placeholder="New lunch"
                    valueBinding="newName" action="createLunch"}}
                </div>
            </div>
        </div>
        </script>
        <%= javascript_include_tag "application" %>
    </body>
</html>
